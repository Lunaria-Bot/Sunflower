
discord.py>=2.3.2

# Postgres async driver
asyncpg>=0.29.0

# Redis async client
redis>=5.0.1

# Logging, scheduling, and utilities
python-dotenv>=1.0.0
aiohttp>=3.9.0
aiosignal>=1.3.1

# Optional: voice support (if ever needed)
PyNaCl>=1.5.0

# Optional: for emoji parsing, markdown, etc.
emoji>=2.10.0
markdownify>=0.11.6

# Optional: for date parsing and formatting
python-dateutil>=2.8.2
